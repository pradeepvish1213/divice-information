import React from 'react';
import './App.css';

function App() {
    return (
        <div className="App" style={{background:'#DDD'}}>

            <h2>IP, Device ID, Device Make, Device Model, OS, OS Version, Browser, Browser Version
            </h2>
        </div>
    );
}

export default App;
